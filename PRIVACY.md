# Ars-note 隐私说明

更新日期：2026-07-12

## 本地数据

Ars-note 是本地优先应用。Vault 文档、图片、AI 记忆、团队任务和本地历史默认保存在用户选择的文件夹或应用数据目录中。

应用不会自动把 Vault 内容发送给 Ars-note 发布者，也不包含广告跟踪、行为分析或自动遥测。

崩溃报告默认只写入本机应用数据目录，不会自动上传。

## AI 服务

只有在用户配置并调用 AI 功能时，Ars-note 才会向对应 AI 服务发送请求。请求可能包含：

- 用户输入的指令
- 当前文档内容
- 为完成任务读取的相关 Vault 文档片段
- 工具调用结果和必要的项目上下文

数据处理方式受用户所选 AI 服务商的隐私条款约束。使用云端 AI 前，请确认团队有权发送相关项目内容。使用本地模型时，数据是否离开本机取决于用户填写的服务地址。

AI API Key 使用 Electron `safeStorage` 交由操作系统加密后保存在应用数据目录；若系统安全存储不可用，密钥只保留在当前运行内存中。旧版本 Vault 根目录中的 `.ai-config.json` 会在首次读取后迁移并删除。

## 自托管同步

启用实时同步或远程备份后，Vault 文件、删除记录、版本历史、团队任务和同步所需元数据会发送到用户配置的自托管服务器。服务器数据由部署者控制。

同步 API Key 使用操作系统安全存储加密。服务器管理网页可能按照浏览器自身规则保存会话信息；不要在公共或不受信任设备上登录管理页。

## 删除数据

- 删除本地 Vault：由用户在文件系统中执行。
- 清除 AI 配置：在 Ars-note 设置中清除 AI 配置。
- 清除同步凭据：在实时同步设置中断开并清除配置。
- 删除服务器数据：由服务器管理员通过管理台、备份策略或服务器存储目录执行。

卸载应用不会自动删除用户主动创建的 Vault。

## 发布者责任

公开发布页面应补充实际发布主体、联系地址、适用地区以及必要的法定隐私条款。本文件只描述当前软件行为，不替代发布者所在地所要求的法律文件。

