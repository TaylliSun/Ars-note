# 变更记录

## 1.5.69 - 2026-07-28

### AI

- AI 请求统一使用 120K token 运行预算，并按中英文内容估算实际上下文占用。
- 长对话超限时自动压缩较早轮次，保留最近对话、当前请求以及旧记录中的文件路径、决定、约束和风险。
- 新增本地 Markdown 分块检索：按标题、路径和正文相关度自动召回所需片段，不再把少数整篇文档粗略塞入上下文。
- Vault 索引增量复用未修改文件；相关 AI Skill 会根据当前请求自动召回并注入。
- AI 返回实际运行上下文用量，界面用量环与主进程限制保持一致。

### 质量

- 增加中文检索、分块召回、超长对话硬预算收敛及关键信息保留测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.68 - 2026-07-28

### AI

- 新增全库 Markdown 精确检索工具，术语或设定变更时会列出所有命中文档、行号、上下文及代码块状态。
- 新增跨文档术语迁移工具，可将“旧称 → 新称”应用到经过语义筛选的文档，并默认保留代码块、历史引用和技术标识。
- 策划设定改名现在遵循“全库检索、影响判定、批量更新、残留复查、变更报告”的闭环，不再只修改相关度最高的少量文档。
- 批量迁移会为每个文件保存本地历史，并为整批操作生成统一回滚快照。

### 质量

- 增加中文等长术语替换、代码块保护、路径约束、任务路由和强制先检索后修改测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.67 - 2026-07-26

### 性能

- 工作区模板和批量建档能力改为按需加载，主入口脚本由约 417 KB 降至约 323 KB。
- AI Vault 上下文扫描改为异步增量索引，未修改的 Markdown 不再重复读取。
- 大型 Vault 中的附件不会进入 AI 文档扫描，连续提问时主进程响应更稳定。

### AI

- AI 请求支持停止生成、五分钟超时和失败后原样重试。
- 请求具有独立 ID 和可靠清理流程，避免迟到响应覆盖新对话状态。
- 核心循环成为独立策划文档类型，补齐系统、数值、UX、技术和制作评审要求。
- 长对话只渲染可见窗口，保留完整历史的同时降低输入与滚动卡顿。

### 质量

- 新增真实 Electron 桌面烟雾测试，自动验证欢迎页、最近 Vault 恢复、Markdown 编辑和自动保存。
- 增加 AI 请求生命周期与增量 Vault 扫描测试。
- 本版本不修改实时同步协议或服务器逻辑；服务器版本保持 1.5.64。

## 1.5.64 - 2026-07-12

### 安全

- AI 与实时同步密钥改由操作系统安全存储加密。
- 自动迁移并删除旧版 Vault 根目录中的明文 `.ai-config.json`。
- 客户端和服务器拒绝实时同步 `.ai-config.json`。
- 服务器启动时清理旧的内部或凭据类实时文件及其历史。
- 公开 Docker Compose 缺少强 API Key 时拒绝启动。
- Electron 渲染器启用沙箱并收紧内容安全策略。
- 外部链接仅允许安全协议。
- Electron、构建工具和运行依赖升级后通过完整零漏洞审计。
- Excalidraw 使用审计通过的版本，字体与语言资源随安装包本地发布，不访问第三方 CDN。

### 服务器管理台

- 管理台改成简洁的传统运维布局。
- 日常状态、实时同步、Vault、团队制作、备份和高级管理分区显示。
- 危险操作集中到高级管理，诊断详情默认折叠。

### 同步

- 保持实时同步协议 v4。
- 保持并发写入串行化、过期写入保护、删除墓碑和服务器历史恢复。

### 发布

- 增加公开发布文档、第三方依赖声明、SHA256 和签名状态审计。
