# 变更记录

## 1.5.98 - 2026-08-28

### 思维导图可读性与自适应工具栏

- 思维导图工具栏重新按历史、模式、创建、结构和缩放分组，所有按钮禁止压缩换行，窄空间下仅操作区独立横向滚动。
- 复制、粘贴、同级排序、删除和快捷键帮助收纳到“更多”菜单，保留完整能力的同时减少视觉噪音。
- 导图大纲在桌面宽度下改为停靠面板，不再覆盖左侧分支；窄屏下自动切回浮层，避免挤压可用画布。
- 初次打开导图采用 72% 可读缩放下限，优先保证节点文字清晰；手动“适应全部内容”仍可缩放到完整全览。
- 大纲与画布继续共用 viewport 实际边界进行坐标换算，拖拽、双击和节点命中不会因停靠布局发生偏移。
- 本版本仅更新客户端到 1.5.98；同步服务器继续使用 1.5.67，无需重新部署。

## 1.5.97 - 2026-08-28

### 品牌无关的旧 Vault 迁移

- 客户端、服务器、测试、脚本和项目文档已清除旧产品名称的全部字面残留，当前源码全文搜索结果为零。
- 旧团队制作台不再依赖品牌文件名识别，改为检查 `type: ars-team-command-center`、标准团队链接结构或非标准 `*-command-center.md` 文件。
- 标准制作台成功生成后，重复文件会安全去重，自定义内容会改名归档到 `.ars-team/legacy/`，迁移过程不丢失用户内容。
- 团队初始化任务改为根据 `07_Unity_Tasks/TeamProductionBootstrap.md` 识别并规范标题；生成式工作流标题根据标准链接结构迁移。
- AI 工具仅接受 `include_team_command_center`，服务器管理台仅使用标准 `.ars-team/team-command-center.md`，不再保留品牌相关参数或路径兜底。
- 更新 14 个传递依赖以修复最新安全审计发现的高危问题，客户端与服务器 `npm audit` 均为 0 个已知漏洞。
- 客户端升级为 1.5.97，同步服务器升级为 1.5.67；更新公网/NAS 服务时请替换新的 `server/dist`，并保留现有 `sync-data`。

## 1.5.96 - 2026-08-28

### Ars-note 产品身份统一

- 设置页、团队工作台、命令面板和内部样式命名统一为 Ars-note，不再把旧产品名称作为当前界面身份显示。
- AI 系统提示与生产工具参数只向模型暴露 Ars-note 名称，避免回答、策划文档和团队文档错误引用其他产品。
- 旧团队控制台文件、旧工具参数和旧任务标题仍可由迁移层识别；加载旧任务时会自动显示为 Ars-note 标准标题，刷新制作台后旧文件的自定义内容会无损改名归档。
- 团队任务文档升级时会把旧工作流标题迁移为 `Ars-note Workflow`，所有新生成链接继续使用 `.ars-team/team-command-center.md`。
- 桌面烟雾测试新增产品身份检查，覆盖 Vault 主界面、团队工作台和设置页，防止旧名称再次出现在可见 UI。
- 本版本仅更新客户端到 1.5.96；同步服务器继续使用 1.5.66。

## 1.5.95 - 2026-08-28

### 全链路发布实测与导出安全

- 新增真实桌面深度冒烟测试，覆盖备份导出、完整与单文件恢复、冲突合并和归档、Canvas 思维导图、知识图谱、AI 请求与取消、数值实验室以及窄屏布局。
- 新增真实 PDF 生成校验；PDF 渲染窗口在成功、取消和异常路径都会销毁，避免连续导出后遗留隐藏窗口。
- Markdown 预览与 HTML/PDF 导出会拒绝 `javascript:`、`data:`、带账号密码的 URL 等危险链接协议，同时继续支持 HTTPS、邮件和相对链接。
- AI 服务密钥新增发布级落盘验证，确保配置文件只保存系统加密内容，不出现明文密钥。
- Excalidraw 现在会保留嵌入图片资源，并在普通自动保存、`Ctrl+S`、快速切换文件和组件卸载时可靠写入最后一帧，不再因 250ms 防抖窗口丢失刚完成的绘制。
- 从团队工作台、图谱、AI 或数值实验室等中央工具点击左侧文件时，会统一返回文件编辑器，避免侧栏已选中文件但中央内容仍停留在工具面板。
- 桌面深度测试新增真实 Excalidraw 鼠标绘制与快速切换、团队任务落盘、任务表/看板切换及重新进入恢复验证。
- 自托管服务器冒烟测试扩展到两个 Vault 的同路径文件隔离及跨 Vault 读取拦截；客户端依赖和服务器生产依赖安全审计均为 0 个已知漏洞。
- 本版本仅更新客户端到 1.5.95；同步服务器继续使用 1.5.66。

## 1.5.94 - 2026-08-21

### 思维导图高效创作与大图稳定性

- 新增主题全文搜索，支持上一个/下一个结果、自动展开折叠祖先、居中定位和结果高亮。
- 新增完整分支复制、剪切和粘贴；可在 Ars-note 内保留层级粘贴，也可把外部缩进大纲直接转换为主题树。
- 新增同级上移/下移、一级分支左右移动，并统一工具栏、节点浮条、右键菜单和键盘操作。
- 新增思维导图快捷键帮助、成功反馈、键盘焦点和无障碍语义，常用操作无需猜测。
- 节点尺寸会按内容在安全范围内自适应，长标题减少截断，同时保持布局稳定。
- 大图层级索引改为一次构建复用，拖拽候选目标按下时缓存；宽型数千节点与深型万级节点布局均避免重复扫描和递归栈溢出。
- 新增分支剪贴板、同级排序、宽型大图和 5,000 层深图回归测试；本版本仅更新客户端，同步服务器继续使用 1.5.66。

## 1.5.93 - 2026-08-14

### 思维导图交互状态机 2.0

- 节点按下与真实拖拽彻底分离：移动超过阈值后才进入拖拽，普通单击不再触发布局保存或节点回弹。
- 文本编辑改为即时更新，完成编辑只生成一条撤销记录；`Esc` 可取消本次修改，`Enter`、`Tab` 可连续创建同级或子主题。
- 方向键改为沿可见树结构导航，`Shift+Tab` 可提升层级；新增、导航和窗口尺寸变化后会自动保持目标节点在视野中。
- 拖拽落点区分“成为子主题”“插入同级之前”“插入同级之后”，并提供边框或插入线反馈；换父级、排序均可一步撤销。
- 删除操作明确区分删除整个分支与仅删除当前节点并保留子级，复制操作可完整复制所选分支。
- 导图大纲新增独立展开/折叠控制，并继续与画布选区和居中定位联动。
- 普通滚轮用于平移画布，`Ctrl/Cmd + 滚轮` 围绕鼠标位置缩放，自动适应视图设置最小缩放保护。
- 新增循环连接、重复父级和无效边清理，避免损坏的 Canvas 数据导致方向计算死循环或界面卡死。
- 思维导图专项测试扩展至拖拽阈值、落点分区、方向导航和异常结构修复。

## 1.5.92 - 2026-08-14

### 飞书式思维导图体验

- 思维导图改为中心主题双向展开，一级分支会根据子树高度自动均衡到左右两侧，后代继承所属分支方向。
- 左侧分支使用反向连接锚点，导图连线移除流程图箭头并采用更清晰的曲线表现。
- 中心主题提供左右悬浮加号，普通节点在分支外侧提供快速新增入口；选中节点显示浮动编辑工具条。
- 新增可折叠导图大纲，可查看层级、快速定位主题，并与画布选中状态联动。
- 支持将主题拖到另一个主题上更换父级，自动阻止循环父子关系并重新整理布局。
- 导图节点改为更紧凑的轻量样式，中心主题重点突出；导图模式隐藏自由画布专用的质量和拆卡工具。
- 导图首次打开或切换模式后自动适应视图，避免双向布局位于当前视口之外。
- 导图专项测试扩展至双向布局、左侧锚点、折叠持久化和拖拽换父级保护；完整测试通过 `140/140`。

## 1.5.91 - 2026-08-13

### 思维导图模式

- Canvas 新增独立的“自由画布 / 思维导图”模式切换，旧 `.canvas` 文件默认保持原有自由布局。
- 思维导图支持 `Tab` 新建子主题、`Enter` 新建同级、`F2` 编辑，以及 `Space` 折叠或展开分支。
- 新增基于父子连接和子树高度的自动树形排版，统一连接方向并避免兄弟分支相互重叠。
- 折叠分支只隐藏后代，不删除节点或连线；模式、折叠状态、撤销历史和实时同步共用现有 Canvas 保存链路。
- 思维导图视图采用更紧凑的节点卡片、中心主题强调、分支控制按钮和专用快捷键提示。
- 新增导图布局回归测试；完整桌面测试通过 `138/138`。

## 1.5.90 - 2026-08-09

### 公网同步安全加固

- 桌面客户端不再把同步 API Key 写入 WebSocket URL，只通过认证请求头发送，避免密钥进入浏览器历史、反向代理访问日志和诊断地址。
- 同步服务器新增 `public` 严格模式：强制 32 字符以上密钥、HTTPS/WSS、受信任代理和来源白名单；认证失败会按地址限速并临时封禁。
- HTTP 服务新增请求头与请求超时限制、统一安全响应头、敏感内容禁缓存和服务端错误脱敏；WebSocket 新增全局/单地址连接上限、消息速率、客户端掩码、帧类型和分片校验。
- 管理后台状态接口会报告当前安全模式、HTTPS 强制状态、查询参数密钥策略和连接限制，方便确认公网部署是否真正处于严格模式。

### 部署与验证

- 新增 `docker-compose.public.yml` 与 `PUBLIC_DEPLOYMENT.md`。公网容器仅绑定 `127.0.0.1:8787`，使用非 root UID/GID、只读根文件系统、无 Linux capabilities，并要求由 HTTPS 反向代理对外提供 443。
- NAS 默认 Compose 同样禁用 URL 密钥、启用认证封禁与容器权限收缩，并将默认 HTTP/WebSocket 单消息上限调整为 64 MB；如需同步更大的单文件，可显式提高对应环境变量。
- 客户端发布脚本现在无论是否跳过测试都会强制重建生产资源，防止上一版本的 `dist` 被误装进新版本安装包。
- 客户端升级为 1.5.90，同步服务器升级为 1.5.66。公网模式需要更新 `server/dist` 和 Compose 配置；升级前必须保留 `sync-data` 并更换已经公开过的旧密钥。

## 1.5.89 - 2026-08-09

### 多项目同步隔离

- 实时同步连接提示改为同时绑定本地 Vault 路径和远程 Vault ID；项目 B 不会再继承项目 A 的服务器目标，旧版无路径记录也只有在 Vault ID 完全一致时才允许迁移。
- 切换项目时先停止旧项目文件 watcher，再读取新项目独立配置；即使两个项目使用同一台 NAS，只有服务器地址和 Vault ID 都一致时才复用现有连接。
- 同步设置、凭据保存和自动连接统一写入带项目作用域的提示，右栏切换项目时清空旧连接字段，并忽略不属于当前 Vault 的异步状态。

### 回归保护

- 新增跨路径隔离、旧配置安全迁移、同服务器不同 Vault 不复用连接等回归测试。
- 未修改服务器存储协议；实时文件、版本历史和备份继续按 Vault ID 分目录隔离。服务器保持 1.5.65，NAS 无需更新。

## 1.5.88 - 2026-08-09

### 右栏首屏性能

- 备份工作区与设置/同步工作区从常驻 `RightPanel` 拆分为独立懒加载模块；普通文档编辑不再预先解析备份历史、恢复工具、同步健康、冲突处理和 AI 设置界面。
- 保留原有状态机、计时器、备份恢复动作和同步协议实现，只移动展示边界；常驻右栏由约 233.9 KB 降至 120.4 KB，减少约 48.5%。
- 备份页独占样式改为 5.45 KB 按需 CSS，全局渲染器 CSS 由约 527.0 KB 降至 521.3 KB；亮暗主题公共卡片和按钮仍由全局主题统一管理。

### 发布可靠性

- 客户端发布校验新增备份页、设置页及备份页 CSS 的独立产物检查，并将常驻右栏预算收紧至 140 KB、全局 CSS 预算收紧至 530 KB。
- Windows 安装包烟雾测试新增备份与设置工作区真实导航和懒加载验证，原有大文档渐进加载、表格坐标命中、单元格编辑、自动保存与数值实验室回归继续保留。

### 兼容性

- 未修改 CodeMirror、Live Preview 表格装饰、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.87 - 2026-08-09

### 右栏轻量化

- 游戏文档雷达与 AI 工作区从常驻 `RightPanel` 拆分为独立懒加载模块；普通编辑时不再提前解析筛选、批量维护、质量分析、聊天和记忆管理界面。
- 保留游戏列表/看板偏好、批量写入刷新、AI 快捷提示折叠、聊天自动滚动、安全自检、操作回滚和记忆管理状态；只调整加载边界，不改变数据与执行行为。
- 生产构建中的常驻右栏由约 266.6 KB 降至 233.9 KB，游戏与 AI 子面板分别为约 18.5 KB 和 17.4 KB。

### 性能发布门禁

- 客户端发布校验新增右栏、游戏面板、AI 面板和渲染器 CSS 体积预算，防止后续功能重新堆回首屏资源。
- Windows 安装包生成后自动运行大 GDD 烟雾测试，覆盖渐进加载、表格坐标命中、单元格编辑、普通输入、自动保存与最长主线程阻塞预算。
- 2,000 文档 Vault 回归测试新增首扫、缓存复用和单文件增量刷新时间上限，避免索引路径悄悄退化为全量重读。

### 兼容性

- 未修改 CodeMirror、Live Preview 表格装饰、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.86 - 2026-08-09

### 大文档质量审计

- 中间 AI 工作区与右侧 AI 面板共享同一套质量审计缓存；同一路径、同一内容的并发检查会合并为一次，内容变化后自动失效，手动重新检测只绕过一次缓存。
- 本地策划质量计算移入独立 Web Worker，超大文档抽样不再占用 Electron 主线程；Worker 不可用、异常或超时时自动降级到原有 IPC 审计。
- 新增有界 LRU 缓存、失败不缓存和并发去重回归测试，避免长时间使用后内存无限增长或错误结果滞留。

### 右栏按需加载

- 模板浏览器、完整模板数据和制作指挥中心改为进入对应页面后再加载；`RightPanel` 生产块由约 299.8 KB 降至 266.6 KB。
- 发布校验新增模板面板、制作指挥中心和质量审计 Worker 独立产物门禁，防止后续改动意外退回首屏打包。

### 兼容性

- 未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.85 - 2026-08-08

### 策划质量工作流

- AI 对话区新增当前文档质量卡，本地检测策划类型、专业成熟度和中英文自然度，不调用模型、不消耗额度；明确列出优先缺口、已有能力和模板化写作痕迹。
- 质量卡同时适配右侧紧凑面板与中间 AI 工作区，可一键按缺口专业化、发起只读评审或执行中英文去 AI 化；超大文档自动抽样并明确标注，不把抽样结果冒充完整通过。
- 专业路由优先识别文件名和主标题，避免系统策划仅因正文包含 UI 状态就误选 UX 专家。

### 写入可信度

- 中英文改写新增原文忠实度门禁，逐项核对数字与单位、Wiki 链接、URL、任务 ID、Frontmatter、代码、标题层级和表格结构；来源读取不完整时不会冒险覆盖原文。
- 策划数值新增证据门禁：用户明确需求与 AI 实际读取的项目文档共同构成证据；无来源的新数值必须标为暂定、假设或原型参数，并写明测量方式、调参负责人、验收门槛和决策点。
- 专业策划写入继续要求事实、决策、假设和开放问题分离，缺少可执行规则、边界恢复、制作交接或验收证据时拒绝低质量落盘。

### 质量与兼容性

- 新增事实忠实度、数字证据、策划成熟度报告、超大文档抽样和学科主标题优先级回归测试。
- 未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.84 - 2026-08-08

### 专业策划写作

- 内置 AI 新增证据、决策、专业分工、反向审查、编辑五遍成稿流程，并区分草稿、可评审和可实现三个成熟度层级。
- 策划写入按 GDD、系统、数值、战斗、关卡、叙事、UX、技术和美术分别审查；缺少运行规则、数据或资产交付、失败恢复、边界条件、验收证据和责任交接时不会覆盖原文。
- 新增“专业化当前策划”快捷入口：先读取当前权威文档和必要项目证据，再原位收敛方案，不另建 v2、final、优化版或配套画布。

### 中英文去 AI 化

- AI 会先识别中文、英文或混合文本，并根据策划、制作人、程序、美术或叙事等真实作者身份调整语气，而不是统一套用客服或咨询报告文风。
- 中文重点清理机械的首先/其次/此外、翻译腔“通过...从而...”、口号式四字词和空泛的“提升玩家体验”；英文重点清理模板连接词、膨胀形容词、名词化被动句、三段式列表和通用结尾。
- 新增自然度写入门禁与安全清理，保留事实、数字、专有名词、不确定性、Frontmatter、代码、表格、Markdown 链接、Wiki 链接、任务 ID 和验收条件；自然度不足时要求 AI 重写，不保存低质量结果。
- AI 面板新增“中英去 AI 化”快捷入口，并在保存前执行原文忠实度核对。

### 质量与兼容性

- 新增语言识别、中文模板腔、英文营销腔、Markdown 保护、专业策划分工路由、弱文档拦截和成熟文档放行回归测试。
- 未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.83 - 2026-08-08

### GDD 逻辑架构

- 完整 GDD 改为因果链驱动：玩家承诺、即时目标、选择与行动、规则结算、反馈与产出、资源去向、成长解锁、下一轮决策和重入条件必须前后闭环。
- 明确战斗、采集、建造、探索和对话只是核心循环中的行动或子系统，不能再用单个功能名代替完整核心循环。
- 新建 GDD 模板按体验层级、核心循环、系统依赖、成长经济、内容推进、失败恢复、反馈生产和验证顺序组织，并为每个主要系统补充输入、输出、依赖和验收契约。

### AI 质量门禁

- Agent 在写入完整 GDD 前会自动审查核心循环、资源产消、成长回流、多时间尺度、失败恢复、系统拓扑和原型验证；缺少关键因果链接时会拒绝低质量落盘并要求重写同一权威文档。
- GDD 快捷生成和专业策划路由新增完整因果骨架、系统追溯表与范围收敛要求，同时保留“重大决策比较更简单方案”的成本控制规则。
- 新增弱 GDD 拦截、完整闭环放行、模板结构和提示注入回归测试。

### 兼容性

- 未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.82 - 2026-08-05

### 数值分析工作流

- 数值实验室新增参数敏感性分析，可选择关键输入与 ±10% 至 ±50% 范围，自动计算输出摆幅并标记低、中、高敏感。
- 新增多方案对比：当前方案可与最多三个同类型已保存方案叠加曲线，并以当前方案为基准显示关键指标差值。
- 新增 CSV 导出，趋势、敏感性和方案对比视图均可导出完整数据并继续在 Excel 中分析。
- 成长曲线切换线性、复合或幂函数时会自动选择对应的有效敏感参数，避免生成没有意义的平坦分析。

### 质量与兼容性

- 新增敏感性中点一致性、单调变化与非法字段保护测试；数值分析继续作为独立懒加载模块。
- 未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.81 - 2026-08-04

### 数值实验室

- 左侧工具栏新增独立的数值实验室，可从工具栏或命令面板进入，不占用文件浏览器空间。
- 内置成长曲线、战斗伤害、经济产消和掉落概率四类模型，参数修改后即时计算关键指标、趋势图和抽样明细。
- 掉落模型使用精确概率状态计算，支持硬保底、至少获得一次概率、期望数量和平均抽取次数；战斗模型覆盖减伤、暴击、多段伤害、DPS 与击杀时间。
- 数值方案按 Vault 独立保存到 `.ars-team/balance-lab.json`，支持 Live Sync、载入、更新、删除，并可复制或导出 Markdown 分析报告。
- 工作台适配深色、亮色与窄屏布局；数值算法新增边界回归测试。

### 兼容性

- 数值实验室与编辑器渲染链路完全隔离，未修改 CodeMirror、Live Preview 表格、鼠标坐标模型或同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.80 - 2026-08-03

### 无限加载兜底

- 文档加载提示改为顶部非阻塞状态条，不再覆盖或拦截编辑区；即使提示尚未关闭，也可以看到已经装载的正文并继续操作界面。
- 每次提示最多显示 8 秒，完成事件异常或窗口调度受限时会自动关闭，不会无限转圈。
- 文件读取增加 20 秒明确超时；读取失败会关闭提示并显示具体文件名，不再只在开发者控制台静默报错。
- 超大文档仍使用分段装载。9 MB、约 6.4 万行的表格密集策划文档完整装载约 1.14 秒，编辑与保存回归通过。

### 兼容性

- 未修改表格行高、Live Preview 鼠标坐标模型和同步协议；服务器保持 1.5.65，NAS 无需更新。

## 1.5.79 - 2026-08-03

### 文档加载回归修复

- 文档加载提示从 `App` 根状态中移出，改为独立事件驱动组件；显示或隐藏提示时不再重复渲染文件树、工具栏、右侧面板和整个工作区。
- 移除全屏 `backdrop-filter`、高斯模糊与饱和度合成，降低集成显卡和远程桌面环境下的 GPU/主线程压力。
- 移除首次打开人为增加的 48 ms 等待，仅让出一个零延迟任务供浏览器绘制反馈；已有编辑器仍只在真正慢于 90 ms 时显示提示。
- 加载动画保留文件名、旋转状态和进度反馈，但使用独立轻量图层，避免“为了显示加载动画反而更容易未响应”。

### 回归验证

- 常规文档冒烟测试的主线程长任务总量由上一实现约 508 ms 降至约 459 ms；大型策划分段装载约 27 ms。
- 加载反馈出现/关闭、大型表格滚动、真实鼠标坐标命中、编辑和自动保存继续通过；服务器保持 1.5.65，NAS 无需更新。

## 1.5.78 - 2026-08-03

### 文档打开反馈

- 首次创建 Markdown 编辑器前先绘制文档加载遮罩，显示当前文件名、编辑器准备状态、旋转指示和连续进度条，避免初始化期间表现为点击无响应。
- 已有编辑器时采用 90 ms 延迟显示策略：快速切换不会闪烁，只有读取或布局较慢时才出现加载反馈。
- 加载层由编辑器 `onReady` 信号关闭；大型文档会等待分段正文完整装载，小文档会在 CodeMirror 可编辑后立即关闭，不使用猜测式固定倒计时。
- 加载期间遮罩覆盖编辑区域并阻止误点击，背景保留当前工作上下文并轻微虚化；亮色、深色和减少动态效果模式均可用。

### 回归验证

- 桌面冒烟测试现在要求加载反馈确实出现并在编辑器就绪后消失，同时继续验证大型表格滚动、真实鼠标坐标点击、编辑和自动保存。
- 本版本没有修改表格行高、替换范围或鼠标坐标模型；实时同步协议和服务器仍保持 1.5.65，NAS 无需更新。

## 1.5.77 - 2026-08-03

### 大型策划文档打开性能

- 超过约 120 KB 的 Markdown 策划文档改为分段装载：先显示首段和明确的载入状态，再把剩余正文按小任务追加，避免一次布局整篇文档导致界面长时间无响应。
- 加载期间编辑区域会暂时失焦并禁止输入，完整正文就绪后自动恢复，避免误操作把半份文档写回磁盘。
- 大型文档的 Live Preview 只初始化首屏附近内容；滚动到新区域时按需补充 Markdown 装饰，并保留已访问区域，Wiki 链接、粗体、代码和表格无需等待整篇扫描。
- 光标移动和滚动只重建受影响的局部装饰，不再随着浏览范围增加而反复扫描此前所有内容。

### 性能与回归

- 158 KB 表格密集型策划文档的实际切换状态建立约 33 ms，后续完整装载约 71 ms，单个追加任务最高约 12 ms。
- 桌面冒烟测试新增“大文档滚动到底再返回顶部”和真实鼠标坐标命中表格单元格验证，并继续覆盖表格编辑及自动保存。

### 兼容性

- 表格仍由 CodeMirror `StateField` 渲染，未修改表格行高、源码替换范围、单元格交互或鼠标坐标模型，不会重新引入上下错位。
- 本版本仅更新桌面客户端，实时同步协议与服务器保持 1.5.65，NAS 无需更新。

## 1.5.76 - 2026-08-03

### 团队任务详情体验

- 团队工作台新增任务详情抽屉，可从“团队下一项”、今日任务、协作待办、任务表和看板直接打开，不再需要为了修改一个字段跳进宽大的完整表格。
- 抽屉集中编辑负责人、状态、优先级、预计工时、日期、关联文档、交付物、依赖、阻塞、验收标准和内部备注，并继续复用现有防抖自动保存。
- 抽屉内加入工时进度、交付时间、最近工时/验收动态、开始或结束计时、提交或处理验收以及带确认的删除操作；支持 `Esc`、遮罩和关闭按钮退出。
- 团队区会记住上次使用的视图、排序方式、状态筛选和成员筛选，重新打开或重启后可继续之前的工作位置。
- 详情抽屉补齐深色、亮色、窄屏和减少动态效果模式，移动端会切换为全宽布局。

### 兼容性

- 本版本仅更新桌面端团队交互，服务器保持 1.5.65，NAS 无需更新；未修改编辑器、Live Preview 表格或鼠标坐标模型。

## 1.5.75 - 2026-08-03

### 团队协作待办台

- 团队工作台新增“协作待办”视图，集中显示待验收、阻塞、逾期、未分配以及缺任务文档/验收标准的工作，不再需要在任务表和多个生产报告之间反复查找。
- 待办卡片支持直接通过或退回验收、解除阻塞、顺延三天、按当前负载智能分配负责人、生成或打开任务文档，并可一键跳转到完整任务明细。
- 智能分配会优先避开有阻塞、逾期和高剩余工时的成员；待办总数按任务去重，单个任务仍可出现在多个风险队列中，便于完整闭环。

### Ars-note 团队制作台

- 新生成的团队入口统一改为 `.ars-team/team-command-center.md`，任务文档、AI 提示词和服务器管理页不再使用其他产品名称。
- 旧版团队制作台仍可由客户端识别，现有 Vault 不需要手动改名；下一次“一键生产刷新”会生成新的标准入口。
- 客户端版本升级为 1.5.75，同步服务器升级为 1.5.65；服务器 Docker 配置不变，只需更新 `server` 构建产物后重新部署容器。

### 兼容性

- 本版本未修改 Markdown 编辑器、Live Preview 表格 Widget、行高或鼠标坐标模型，不会影响此前修复的鼠标上下错位问题。

## 1.5.74 - 2026-08-02

### 大策划文档切换与表格性能

- 文档切换不再通过旧编辑器状态执行整篇替换事务，而是为目标文件建立独立 CodeMirror 状态；真实 `SystemDesign.md` 切换处理由约 81 ms 降至约 20 ms，同时隔离不同文件的撤销历史。
- 表格单元格改为点击后才进入可编辑状态，未激活单元格只渲染展示内容，减少表格密集文档首次挂载时的 DOM 监听器与布局成本。
- 已被表格 Widget 覆盖的源码行不再重复生成粗体、Wiki 链接和颜色 Decoration，表格 Widget 相等判断改为轻量逐项比较。
- Live Preview 在普通文本光标移动时复用已有 Decoration，避免无意义的整篇重建。
- 增加真实文档切换、大型表格文档、表格单元格编辑保存、主线程长任务和编辑器阶段计时的桌面烟雾测试。

### 兼容性

- 没有改动表格行高、源码替换范围或 CodeMirror 鼠标坐标模型，不会重新引入上下错位；同步服务器版本保持 1.5.64，无需更新 NAS 服务端。

## 1.5.73 - 2026-08-02

### 大文档编辑性能

- Live Preview 不再因普通段落中的点击、选区和无关编辑器事务重建整篇装饰，减少大策划文档中的主线程长任务。
- 右侧 Markdown 预览只在预览页可见时解析正文；其他右侧页面不再因编辑正文而重复执行全文渲染。
- AI 上下文用量只在 AI 工作区可见时计算，避免打开文档和日常输入时提前拼装大段提示词。
- 分栏预览、右侧大纲和状态栏统计改为低优先级正文更新，编辑器输入优先响应。
- 右侧面板与状态栏加入稳定渲染边界，避免无关正文更新重建大型 UI 子树。
- Electron 文件读取改为异步 I/O，打开较大 Markdown 文件时不再同步阻塞主进程。

### 兼容性

- 未改动 Live Preview 的表格 Widget、行高、替换范围和鼠标坐标模型；同步服务器版本保持 1.5.64。

## 1.5.72 - 2026-08-01

### 大型 Vault 启动性能

- 文件树改为异步遍历，并合并工作区恢复与侧栏初始化产生的同一 Vault 并发请求，不再重复扫描磁盘。
- Markdown 索引改为异步增量扫描，首屏显示后再启动；未修改文件直接复用内存缓存。
- 实时同步自动连接、AI 历史恢复和全文索引错峰启动，避免同时占用 Electron 主进程。
- 自动恢复对话只挂载最近 80 条消息，完整历史文件仍保留在 Vault 中，避免超长对话拖慢工作区。
- 新增 2000 个项目文档加 2000 个隐藏历史文件的大型 Vault 性能回归测试。

### 兼容性

- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.71 - 2026-07-31

### AI 策划治理

- 新增可实时同步的 `.ars-team/design-canon.json`，为 GDD、核心循环、世界观、叙事、数值、UX、技术方案等责任登记唯一正式文档。
- AI 会排除冲突副本、恢复文件、自动生成摘要和 `v2/final/优化版` 等旁生版本，再给出正式文档候选。
- 新增跨文档策划影响分析：识别直接 Wiki 链接、旧术语/规则使用点、下游策划岗位和关联中的团队任务。
- 策划修改落盘前强制执行“读取正式文档登记、读取源文档、影响分析”三步门禁；缺一步都会被运行时拒绝。
- 影响报告要求为高风险关联文档和活动任务给出立即更新、无需语义修改、延期或阻止决定，并明确排期影响。
- 正式文档登记保存在 Vault 内并参与实时同步，不依赖单台电脑的本地配置。

### 质量

- 增加正式文档单一归属、候选过滤、跨文档/任务影响识别和运行时门禁测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.70 - 2026-07-31

### AI

- 项目长期记忆容量提升，并按完整条目压缩；压缩前自动归档，锁定事实和最近决定不会再被字符截断破坏。
- 成员执行和制作人接管模式新增长期项目事实写入工具，明确设定、术语和制作决定可进入 `.ai-memory/MEMORY.md` 并同步。
- Skill 学习改为同类工作重复成功后再晋升，不再为每次工具调用生成随机 `auto-*` 垃圾 Skill。
- 召回 Skill 后自动记录使用次数和成功率，后续排序会参考真实执行效果。
- 对话仅保存界面中的原始消息，不再把注入的 RAG、工作区和系统上下文重复写入历史。
- OpenAI 兼容接口不支持工具协议时，会自动退回普通对话请求并保留明确错误处理。
- 新增主策划范围与版本治理：先按严重度指出逻辑缺口和制作后果，再比较简化方案、创新价值、验证成本与停止条件。
- 策划修订默认原地更新当前正式文档，阻止 AI 擅自创建 `v2`、`final`、`优化版`、副本或第二份竞争规格。
- 新增范围和工期门禁：新增内容必须归类为本期必需、替换现有、延期或拒绝，并报告增改删延、净范围和排期影响。
- 纯策划评审改为强制只读；剧情链缺少下游文档时只列依赖缺口，不再自动把整条制作链全部扩写出来。

### 性能与体验

- 删除不会成功同步的 `live-session.json` 高频镜像，减少长对话时的磁盘写入、IPC 调用和输入卡顿。
- 收紧前端自然文件引用推断，泛化“需求/文档”请求不再额外塞入多个任意全文；主进程分块 RAG 继续负责相关内容召回。
- 策划、叙事、Canvas、线框和润色提示词改为按任务动态装载；普通对话固定上下文 token 降低约 28%。
- AI 权限说明改成当前真实行为：执行模式按明确要求直接操作，并自动保存历史、回收副本、审计与回滚快照。
- 清理旧版重复检索、上下文压缩和任务策略实现，AI 主链路只保留当前模块。

### 质量

- 增加长期记忆保留、重复事实去重、Skill 晋升去重、Skill 效果统计、供应商工具兼容回退、策划收敛、单一正式版本和只读评审测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.69 - 2026-07-28

### AI

- AI 请求统一使用 120K token 运行预算，并按中英文内容估算实际上下文占用。
- 长对话超限时自动压缩较早轮次，保留最近对话、当前请求以及旧记录中的文件路径、决定、约束和风险。
- 新增本地 Markdown 分块检索：按标题、路径和正文相关度自动召回所需片段，不再把少数整篇文档粗略塞入上下文。
- Vault 索引增量复用未修改文件；相关 AI Skill 会根据当前请求自动召回并注入。
- AI 返回实际运行上下文用量，界面用量环与主进程限制保持一致。

### 质量

- 增加中文检索、分块召回、超长对话硬预算收敛及关键信息保留测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.68 - 2026-07-28

### AI

- 新增全库 Markdown 精确检索工具，术语或设定变更时会列出所有命中文档、行号、上下文及代码块状态。
- 新增跨文档术语迁移工具，可将“旧称 → 新称”应用到经过语义筛选的文档，并默认保留代码块、历史引用和技术标识。
- 策划设定改名现在遵循“全库检索、影响判定、批量更新、残留复查、变更报告”的闭环，不再只修改相关度最高的少量文档。
- 批量迁移会为每个文件保存本地历史，并为整批操作生成统一回滚快照。

### 质量

- 增加中文等长术语替换、代码块保护、路径约束、任务路由和强制先检索后修改测试。
- 本版本不修改实时同步协议或服务器逻辑，服务器版本保持 1.5.64。

## 1.5.67 - 2026-07-26

### 性能

- 工作区模板和批量建档能力改为按需加载，主入口脚本由约 417 KB 降至约 323 KB。
- AI Vault 上下文扫描改为异步增量索引，未修改的 Markdown 不再重复读取。
- 大型 Vault 中的附件不会进入 AI 文档扫描，连续提问时主进程响应更稳定。

### AI

- AI 请求支持停止生成、五分钟超时和失败后原样重试。
- 请求具有独立 ID 和可靠清理流程，避免迟到响应覆盖新对话状态。
- 核心循环成为独立策划文档类型，补齐系统、数值、UX、技术和制作评审要求。
- 长对话只渲染可见窗口，保留完整历史的同时降低输入与滚动卡顿。

### 质量

- 新增真实 Electron 桌面烟雾测试，自动验证欢迎页、最近 Vault 恢复、Markdown 编辑和自动保存。
- 增加 AI 请求生命周期与增量 Vault 扫描测试。
- 本版本不修改实时同步协议或服务器逻辑；服务器版本保持 1.5.64。

## 1.5.64 - 2026-07-12

### 安全

- AI 与实时同步密钥改由操作系统安全存储加密。
- 自动迁移并删除旧版 Vault 根目录中的明文 `.ai-config.json`。
- 客户端和服务器拒绝实时同步 `.ai-config.json`。
- 服务器启动时清理旧的内部或凭据类实时文件及其历史。
- 公开 Docker Compose 缺少强 API Key 时拒绝启动。
- Electron 渲染器启用沙箱并收紧内容安全策略。
- 外部链接仅允许安全协议。
- Electron、构建工具和运行依赖升级后通过完整零漏洞审计。
- Excalidraw 使用审计通过的版本，字体与语言资源随安装包本地发布，不访问第三方 CDN。

### 服务器管理台

- 管理台改成简洁的传统运维布局。
- 日常状态、实时同步、Vault、团队制作、备份和高级管理分区显示。
- 危险操作集中到高级管理，诊断详情默认折叠。

### 同步

- 保持实时同步协议 v4。
- 保持并发写入串行化、过期写入保护、删除墓碑和服务器历史恢复。

### 发布

- 增加公开发布文档、第三方依赖声明、SHA256 和签名状态审计。
