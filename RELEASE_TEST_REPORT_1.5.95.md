# Ars-note 1.5.95 发布实测报告

测试日期：2026-08-28
桌面客户端：1.5.95
同步服务器：1.5.66（本轮未修改生产服务器代码）

## 自动化门禁

- 客户端单元与集成测试：153/153 通过。
- 同步服务器协议测试：13/13 通过。
- 隔离自托管 HTTP 实测：16/16 通过。
- 客户端与服务器生产依赖审计：均为 0 个已知漏洞。
- TypeScript 类型检查、生产构建、安装包版本、懒加载模块、CSS/模块尺寸预算和安装包 SHA256 校验通过。

## 真实桌面交互

- Markdown：打开、输入、自动保存、长行换行、Live Preview、只读预览和文件切换通过。
- 表格：大文档首尾滚动、真实鼠标坐标命中、进入单元格编辑、退出编辑和落盘通过，没有通过改变行高或跨行替换规避测试。
- Excalidraw：真实鼠标选择矩形工具并绘制；250ms 防抖自动保存、Ctrl+S、绘制后 40ms 内切换文件、重新打开和文档结构保留通过。
- Canvas 思维导图：坐标点击、搜索、Tab 新建子主题、自动整理、同级排序和自动保存通过。
- 知识图谱：节点生成、搜索定位、详情显示和暂停物理模拟通过。
- 团队工作台：新增任务、Vault 落盘、任务表、看板、离开后重新进入恢复通过。
- AI：使用本地假 OpenAI-compatible 服务完成配置、鉴权、连接测试、对话、历史持久化、取消请求和清空；磁盘配置不包含明文密钥。
- 备份与恢复：导出、完整恢复到隔离目录、单文件恢复、完整性校验、未变化跳过、保留数量和越界删除拦截通过。
- 同步冲突：扫描、双版本预览、合并草稿、确认写回、冲突归档和路径穿越拦截通过。
- PDF：真实生成 PDF，校验 `%PDF-` 文件头、文件大小、样式 HTML 和安全文件名通过。
- 响应式：900x640、1180x720、1920x1080 下无全局溢出，编辑区、导航栏和右侧抽屉保持可用。
- 数值实验室：战斗模型、Vault 方案保存、敏感性分析和多方案对比通过。

## 性能边界

- 120 节、表格密集大策划：文档完整就绪约 26-30ms；Live Preview 单次装饰约 11-13ms；最大主线程长任务约 246-283ms。
- 2,000 文档 Vault，另含 2,000 个隐藏历史文件和 240 条 AI 历史：编辑器就绪约 249ms；最大主线程长任务约 239ms。
- 全功能组合烟雾测试：最大主线程长任务约 234-245ms，没有未响应或无限加载。

## 本轮发现并修复

- PDF 隐藏窗口在异常或取消路径可能残留，并且渲染器失焦时可能错误报告 `No window`。
- Markdown、表格和旧编辑器链接缺少统一危险协议过滤。
- Excalidraw 保存会丢失 `files` 内嵌资源，快速切换或关闭可能丢失防抖窗口内的最后一帧。
- 中央团队工作台打开时点击左侧文件只改变选中状态，没有返回文件编辑器。
- Windows PowerShell 环境缺少 `Get-FileHash` 时发布校验无法生成 SHA256。
- electron-builder 刚结束时首次启动烟雾测试可能遇到瞬时 Windows 原生退出；发布脚本现在等待后完整重试一次，第二次失败仍会阻止发布。

## 仍需真实环境人工验证

- 两台真实电脑经公网 HTTPS/WSS、DDNS 或反向代理连接 NAS，覆盖断网数小时、路由器换 IP、睡眠唤醒和同时离线编辑。
- OpenAI、DeepSeek、通义等真实供应商的限流、超时、工具调用差异和长回答中断。
- 干净 Windows 用户、覆盖升级、修改安装目录、卸载以及 SmartScreen；当前安装包尚未使用受信任 Authenticode 证书签名。
- 125%/150%/200% DPI、4K 多屏、触控板、中文/日文输入法和远程桌面的人工视觉与输入检查。
- 进程被强制断电或磁盘写满时的文件系统级恢复。应用已有历史与备份保护，但自动化无法等价模拟真实硬件掉电。

## 发布物

- 安装包：`release-1.5.95/Ars-note-Setup-1.5.95.exe`
- 清单：`release-1.5.95/client-release-manifest.json`
- SHA256：`e10f25c7fc0e1e3c3be53163dd0839ca4440f5d9e9106dab316c79c4e69d0fa8`
